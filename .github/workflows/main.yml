name: simco Project Publish to cPanel

on:
  push:
    branches:
      - master

jobs:
  FTP-Deploy-Action:
    name: FTP-Deploy-Action codes
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: FTP Deploy
        uses: SamKirkland/FTP-Deploy-Action@4.0.0
        with:
          server: "ftp.chapelsage.space"
          username: "ftp_simco@simcoenterprise.com"
          password: "[6KT#uEElt$m"
          server-dir: "/home/chapmpug/simcoenterprise.com/"
          args: --debug
